{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd web && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd web && python manage.py migrate --noinput && python manage.py load_exercises && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
